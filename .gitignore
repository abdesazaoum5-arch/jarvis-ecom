node_modules/
dist/
# Compiled browser bundle: regenerated by `npm run build` and by `npm start`.
apps/command-center/public/js/
# Live research queue and screenshots: operational state, not source.
jarvis/bridge/
jarvis/screenshots/
jarvis/state/*.json
jarvis/logs/
data/**/*.json
!data/**/.gitkeep
.env
.env.*
*.log

# The template is safe to commit; it holds no values.
!.env.example

# Storefronts JARVIS builds: generated output, not source.
jarvis/sites/
